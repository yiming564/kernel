
include ../include/rules.make

.PHONY: clean clean_backup

all:

image_main:
	cp kernel.bin /mnt
	
clean_backup:
	rm -rf *~ *.swp *.swo *.swa *.log
	
clean: 
	@$(MAKE) -C . clean_backup
	rm -rf *.bin *.o *.obj
